{{- if and .Values.frontend.autoscaling.enabled .Values.frontend.autoscaling.createHpa }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "portkey.fullname" . }}-{{ .Values.frontend.name }}
  labels:
    {{- include "portkey.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "portkey.fullname" . }}-{{ .Values.frontend.name }}
  minReplicas: {{ .Values.frontend.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.frontend.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.frontend.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.frontend.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.frontend.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.frontend.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
  behavior:
    scaleUp:
      stabilizationWindowSeconds: {{ .Values.frontend.autoscaling.behavior.scaleUp.stabilizationWindowSeconds | default 0 }}
      policies:
      - type: Pods
        value: {{ .Values.frontend.autoscaling.behavior.scaleUp.podScaleUpValue | default 4 }}
        periodSeconds: {{ .Values.frontend.autoscaling.behavior.scaleUp.periodSeconds | default 15 }}
      - type: Percent
        value: {{ .Values.frontend.autoscaling.behavior.scaleUp.percentScaleUpValue | default 100 }}
        periodSeconds: {{ .Values.frontend.autoscaling.behavior.scaleUp.periodSeconds | default 15 }}
    scaleDown:
      stabilizationWindowSeconds: {{ .Values.frontend.autoscaling.behavior.scaleDown.stabilizationWindowSeconds | default 300 }}
      policies:
      - type: Pods
        value: {{ .Values.frontend.autoscaling.behavior.scaleDown.podScaleDownValue | default 1 }}
        periodSeconds: {{ .Values.frontend.autoscaling.behavior.scaleDown.periodSeconds | default 60 }}
{{- end }}