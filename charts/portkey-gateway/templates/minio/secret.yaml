{{- if and .Values.minio.enabled .Values.minio.authKey.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "minio.secretName" . }}
  labels:
    {{- include "minio.labels" . | nindent 4 }}
type: Opaque
data:
  accessKey: {{ .Values.minio.authKey.accessKey | b64enc }}
  secretKey: {{ .Values.minio.authKey.secretKey | b64enc }}
{{- end }}