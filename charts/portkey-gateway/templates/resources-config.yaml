apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "portkeyenterprise.fullname" . }}-resource-config
  labels:
    {{- include "portkeyenterprise.labels" . | nindent 4 }}
data:
  document_db.pem: |
    -----BEGIN CERTIFICATE-----
    ____
    -----END CERTIFICATE-----